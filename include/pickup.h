#ifndef __PICKUP_H__
#define	__PICKUP_H__

#include "simple_json.h"
#include "entity.h"
 //DIFFERENT PICKUP ITEMS

Entity *legendary_spawn(Vector3D position);//gain lengendary attack 
Entity* heal_spawn(Vector3D position);//healing for ditto
Entity* radius_spawn(Vector3D position);//farther radius for spotting
Entity* fly_spawn(Vector3D position);//gain abilitity to fly
Entity* speed_spawn(Vector3D position);//movement speed







#endif